{
  "name": "flashcard-android",
  "version": "1.2.1",
  "private": true,
  "scripts": {
    "sync:web": "node scripts/sync-web.mjs",
    "android:sync": "npm run sync:web && cap sync android",
    "android:build": "npm run android:sync && cd android && .\\gradlew.bat assembleDebug",
    "android:bundle": "npm run android:sync && cd android && .\\gradlew.bat bundleRelease",
    "android:build:unix": "npm run android:sync && cd android && ./gradlew assembleDebug",
    "android:bundle:unix": "npm run android:sync && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  },
  "overrides": {
    "uuid": "11.1.1"
  }
}
